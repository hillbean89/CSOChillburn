import random

names={"Corin", "Scott", "Elizabeth", "Lydia"}

def scott():
    rand_name = random.choice(names)
    return rand_name

name=soctt()
print(name) #this is the whole prgoram, aside from definitions
                #just a single function call
